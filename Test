<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Brooks's Proxy</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <style>
    :root {
      --bg-main: #050814;
      --bg-panel: #0b1020;
      --accent: #5b8dff;
      --accent-soft: rgba(91, 141, 255, 0.25);
      --text-main: #f5f7ff;
      --text-muted: #98a0c0;
      --danger: #ff5c7a;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    }

    body {
      min-height: 100vh;
      background: radial-gradient(circle at top, #1b2550 0, #050814 55%, #000000 100%);
      color: var(--text-main);
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 24px;
    }

    .shell {
      width: 100%;
      max-width: 720px;
      background: linear-gradient(145deg, rgba(255,255,255,0.04), rgba(0,0,0,0.6));
      border-radius: 18px;
      border: 1px solid rgba(120, 140, 255, 0.4);
      box-shadow:
        0 0 60px rgba(40, 90, 255, 0.35),
        0 0 140px rgba(0, 0, 0, 0.9);
      padding: 26px 26px 32px;
      position: relative;
      overflow: hidden;
    }

    .shell::before,
    .shell::after {
      content: "";
      position: absolute;
      border-radius: 999px;
      filter: blur(32px);
      opacity: 0.65;
      pointer-events: none;
    }

    .shell::before {
      width: 220px;
      height: 220px;
      background: radial-gradient(circle, rgba(91,141,255,0.8), transparent 70%);
      top: -60px;
      right: -40px;
    }

    .shell::after {
      width: 220px;
      height: 220px;
      background: radial-gradient(circle, rgba(255,92,122,0.7), transparent 70%);
      bottom: -80px;
      left: -40px;
    }

    header {
      position: relative;
      z-index: 1;
      margin-bottom: 24px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
    }

    .title-block {
      display: flex;
      flex-direction: column;
      gap: 6px;
    }

    h1 {
      font-size: 1.8rem;
      letter-spacing: 0.03em;
      display: inline-flex;
      align-items: center;
      gap: 8px;
    }

    .badge {
      font-size: 0.7rem;
      text-transform: uppercase;
      letter-spacing: 0.14em;
      padding: 2px 8px;
      border-radius: 999px;
      border: 1px solid rgba(255,255,255,0.2);
      background: rgba(5,8,20,0.7);
      color: var(--text-muted);
    }

    .subtitle {
      font-size: 0.9rem;
      color: var(--text-muted);
    }

    .orbit {
      position: relative;
      width: 70px;
      height: 70px;
    }

    .planet {
      position: absolute;
      inset: 10px;
      border-radius: 50%;
      background: radial-gradient(circle at 30% 20%, #ffffff, #6a8dff 45%, #1b2250 70%);
      box-shadow: 0 0 20px rgba(91,141,255,0.75);
    }

    .orbit-ring {
      position: absolute;
      inset: 0;
      border-radius: 50%;
      border: 1px dashed rgba(180,191,255,0.4);
      transform: rotate(-15deg);
    }

    .orbit-dot {
      position: absolute;
      width: 6px;
      height: 6px;
      border-radius: 50%;
      background: #ffd479;
      top: -3px;
      left: 50%;
      transform: translateX(-50%);
      box-shadow: 0 0 12px rgba(255,212,121,0.9);
    }

    .panel {
      position: relative;
      z-index: 1;
      background: rgba(5,8,20,0.88);
      border-radius: 14px;
      padding: 18px 18px 20px;
      border: 1px solid rgba(91,141,255,0.4);
      box-shadow: inset 0 0 0 1px rgba(0,0,0,0.6);
    }

    .panel-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 14px;
      font-size: 0.78rem;
      text-transform: uppercase;
      letter-spacing: 0.14em;
      color: var(--text-muted);
    }

    .status {
      display: inline-flex;
      align-items: center;
      gap: 6px;
    }

    .status-dot {
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background: #35d799;
      box-shadow: 0 0 10px rgba(53,215,153,0.9);
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    .input-row {
      display: flex;
      gap: 8px;
      align-items: stretch;
    }

    .prefix {
      display: inline-flex;
      align-items: center;
      padding: 0 10px;
      font-size: 0.78rem;
      border-radius: 999px;
      background: rgba(30,37,79,0.95);
      color: var(--text-muted);
      border: 1px solid rgba(124,144,255,0.7);
      white-space: nowrap;
    }

    .search-input {
      flex: 1;
      border-radius: 999px;
      border: 1px solid rgba(124,144,255,0.75);
      background: radial-gradient(circle at top left, rgba(91,141,255,0.22), rgba(5,8,20,0.96));
      color: var(--text-main);
      padding: 11px 14px;
      font-size: 0.98rem;
      outline: none;
      box-shadow: 0 0 0 1px rgba(0,0,0,0.7), 0 0 20px var(--accent-soft);
    }

    .search-input::placeholder {
      color: rgba(152,160,192,0.85);
    }

    .search-input:focus {
      border-color: #9fb4ff;
      box-shadow:
        0 0 0 1px rgba(0,0,0,0.9),
        0 0 28px rgba(91,141,255,0.6);
    }

    .go-btn {
      border-radius: 999px;
      border: none;
      padding: 0 18px;
      background: radial-gradient(circle at top left, #8eb3ff, #4160ff);
      color: #050814;
      font-weight: 600;
      font-size: 0.9rem;
      cursor: pointer;
      display: inline-flex;
      align-items: center;
      gap: 6px;
      box-shadow:
        0 0 18px rgba(91,141,255,0.9),
        0 0 0 1px rgba(0,0,0,0.7);
      white-space: nowrap;
    }

    .go-btn span.icon {
      font-size: 1.1rem;
      translate: 0 0;
      transition: translate 0.12s ease-out;
    }

    .go-btn:hover .icon {
      translate: 2px 0;
    }

    .hint-row {
      display: flex;
      justify-content: space-between;
      gap: 10px;
      color: var(--text-muted);
      font-size: 0.78rem;
      margin-top: 4px;
      flex-wrap: wrap;
    }

    .chip {
      padding: 2px 8px;
      border-radius: 999px;
      border: 1px solid rgba(91,141,255,0.55);
      background: rgba(10,16,36,0.95);
      color: #bac3ff;
    }

    .danger {
      color: var(--danger);
    }

    @media (max-width: 580px) {
      .shell { padding: 20px 18px 24px; }
      header { flex-direction: row; align-items: center; }
      h1 { font-size: 1.45rem; }
      .panel { padding: 16px 14px 18px; }
      .input-row { flex-direction: column; align-items: stretch; }
      .go-btn { justify-content: center; padding-block: 8px; }
      .prefix { justify-content: center; }
    }
  </style>
</head>
<body>
  <div class="shell">
    <header>
      <div class="title-block">
        <div class="badge">Space-dark portal</div>
        <h1>Brooks's Proxy</h1>
        <p class="subtitle">
          Enter any URL or search terms and jump through via DuckDuckGo.
        </p>
      </div>
      <div class="orbit" aria-hidden="true">
        <div class="orbit-ring"></div>
        <div class="planet"></div>
        <div class="orbit-dot"></div>
      </div>
    </header>

    <section class="panel">
      <div class="panel-header">
        <div class="status">
          <span class="status-dot"></span>
          <span>Relay online</span>
        </div>
        <span>DuckDuckGo tunnel</span>
      </div>

      <!--
        This form sends the user's input to DuckDuckGo as the "q" parameter.
        DuckDuckGo automatically treats full URLs as queries and shows results or an instant redirect.
      -->
      <form action="https://duckduckgo.com/" method="GET" target="_blank" rel="noopener">
        <div class="input-row">
          <span class="prefix">https://duckduckgo.com/?q=</span>
          <input
            class="search-input"
            type="text"
            name="q"
            placeholder="Type a URL (https://example.com) or any search…"
            autocomplete="off"
            required
          >
          <button class="go-btn" type="submit">
            Launch
            <span class="icon">➜</span>
          </button>
        </div>

        <div class="hint-row">
          <span>Tip: Paste a full URL or just describe what you want.</span>
          <span class="chip">Private search relay</span>
        </div>
      </form>
    </section>
  </div>
</body>
</html>
